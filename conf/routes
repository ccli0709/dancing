# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()
POST     /login                      controllers.Application.validate()
GET     /login                      controllers.Application.login()
GET     /logout                     controllers.Application.logout()

GET     /class/weeks                controllers.WeekClassMaster.index()
GET     /class/week                 controllers.WeekClassDetail.aaa()
GET     /class/week/:id             controllers.WeekClassDetail.index(id: Long)
POST    /class/week/:id             controllers.WeekClassDetail.post(id: Long)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
